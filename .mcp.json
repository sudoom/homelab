{
  "mcpServers": {
    "kubernetes": {
      "command": "npx",
      "args": ["-y", "mcp-server-kubernetes@4.1.7"],
      "env": {
        "KUBECONFIG_PATH": "${HOME}/.kube/config-readonly",
        "ALLOW_ONLY_NON_DESTRUCTIVE_TOOLS": "true"
      }
    }
  }
}
