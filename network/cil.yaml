apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "pool1"
spec:
  blocks:
  - start: "192.168.1.210"
    stop: "192.168.1.220"
  # serviceSelector:
  #   matchExpressions:
  #     - {key: "io.kubernetes.service.name", operator: NotIn, values: [transmission]}
---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumL2AnnouncementPolicy
metadata:
  name: policy1
spec:
  # serviceSelector:
  #   matchExpressions:
  #     - {key: "io.kubernetes.service.name", operator: NotIn, values: [transmission]}
  # nodeSelector:
  #     matchExpressions:
  #     - key: kubernetes.io/hostname=node2
  #       operator: Exists
  loadBalancerIPs: true
  externalIPs: true
