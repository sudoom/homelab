apiVersion: v1
kind: Service
metadata:
  name: shelly-exporter
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: shelly-exporter
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: shelly-exporter
  ports:
    - name: metrics
      port: {{ .Values.port }}
      targetPort: metrics
      protocol: TCP
