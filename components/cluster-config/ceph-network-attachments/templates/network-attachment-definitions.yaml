{{- range .Values.attachments }}
---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: {{ . }}
  namespace: {{ $.Values.namespace }}
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "type": "macvlan",
      "master": {{ $.Values.masterInterface | quote }},
      "mode": "bridge",
      "mtu": {{ $.Values.mtu }},
      "ipam": {
        "type": "whereabouts",
        "network_name": {{ $.Values.ipam.networkName | quote }},
        "range": {{ $.Values.ipam.range | quote }},
        "range_start": {{ $.Values.ipam.rangeStart | quote }},
        "range_end": {{ $.Values.ipam.rangeEnd | quote }},
        "exclude": [
          {{- range $i, $cidr := $.Values.ipam.exclude }}
          {{- if $i }},{{ end }}
          {{ $cidr | quote }}
          {{- end }}
        ]
      }
    }
{{- end }}
