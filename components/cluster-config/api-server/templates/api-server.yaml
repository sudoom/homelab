---
apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
      - names:
          - {{ .Values.apiDomain }}
        servingCertificate:
          name: {{ .Values.certSecretName }}
