apiVersion: v1
kind: Service
metadata:
  name: mikrotik-exporter
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: mikrotik-exporter
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mikrotik-exporter
  ports:
    - name: metrics
      port: {{ .Values.port }}
      targetPort: metrics
      protocol: TCP
