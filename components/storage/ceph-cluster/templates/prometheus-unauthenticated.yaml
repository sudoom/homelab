apiVersion: v1
kind: Service
metadata:
  name: prometheus-unauthenticated
  namespace: openshift-monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: web
      port: 9090
      targetPort: 9090
      protocol: TCP
